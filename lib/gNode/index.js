const unzipAsValidation = require('./unzip-as-validation')

/**
 * @typedef {Tuple< String, Array<String>, Array<String>, OnVisit >} UnzippedGNode
 */

 /**
 * @typedef {Tuple< Array<String>, Array<Array<String>>, Array<Array<String>>, Array<OnVisit> >} GParts
 */

module.exports = {
  unzipAsValidation
}
